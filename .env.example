# =============================================================================
# Database Configuration
# =============================================================================

# Local development (Docker Compose PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vc_compliance?schema=public"

# Vercel Postgres (automatically set when a database is linked in Vercel)
# These are set automatically by Vercel -- do NOT set them manually in Vercel.
# Only set them locally if you want to run CLI commands against the production database.
# POSTGRES_URL="postgres://..."             # Pooled connection (used at runtime)
# POSTGRES_URL_NON_POOLING="postgres://..."  # Direct connection (used by Prisma CLI)

# =============================================================================
# NextAuth
# =============================================================================
NEXTAUTH_SECRET="change-this-to-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# Resend (Email)
# =============================================================================
RESEND_API_KEY="re_your_api_key_here"
EMAIL_FROM="compliance@yourdomain.com"

# =============================================================================
# App
# =============================================================================
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Vercel Deployment Notes ---
# 1. Link a Vercel Postgres database to your project (Vercel Dashboard > Storage)
#    This automatically sets POSTGRES_URL, POSTGRES_URL_NON_POOLING, etc.
# 2. Set NEXTAUTH_URL to your Vercel domain (e.g. https://yourapp.vercel.app)
# 3. Set NEXT_PUBLIC_APP_URL to match NEXTAUTH_URL
# 4. Generate a secure NEXTAUTH_SECRET: openssl rand -base64 32
# 5. Get a Resend API key at https://resend.com
